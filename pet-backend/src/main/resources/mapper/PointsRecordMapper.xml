<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pethome.mapper.PointsRecordMapper">
    
    <!-- 根据用户ID和类型获取积分记录 -->
    <select id="getRecordsByUserIdAndType" resultType="com.pethome.entity.PointsRecord">
        SELECT * FROM points_record
        WHERE user_id = #{userId} AND type = #{type}
        ORDER BY create_time DESC
    </select>
    
    <!-- 获取用户累计获取的积分 -->
    <select id="getTotalEarnedByUserId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(points), 0) FROM points_record
        WHERE user_id = #{userId} AND type = 'earn'
    </select>
    
    <!-- 获取用户累计消耗的积分 -->
    <select id="getTotalSpentByUserId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(points), 0) FROM points_record
        WHERE user_id = #{userId} AND type = 'spend'
    </select>
    
</mapper>
