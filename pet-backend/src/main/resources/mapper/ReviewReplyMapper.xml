<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pethome.mapper.ReviewReplyMapper">

    <select id="findByReviewId" resultType="com.pethome.entity.ReviewReply">
        SELECT * FROM review_reply 
        WHERE review_id = #{reviewId}
        ORDER BY create_time ASC
    </select>
    
    <select id="countByReviewId" resultType="int">
        SELECT COUNT(*) FROM review_reply 
        WHERE review_id = #{reviewId}
    </select>

</mapper>
