<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pethome.mapper.SearchMapper">

    <!-- 搜索商品 -->
    <select id="searchProducts" resultType="com.pethome.entity.Product">
        SELECT * FROM product
        WHERE status = 1
        AND (
            name LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%')
        )
        ORDER BY create_time DESC
    </select>

    <!-- 搜索帖子 -->
    <select id="searchPosts" resultType="com.pethome.entity.Post">
        SELECT * FROM post
        WHERE status = 1
        AND (
            title LIKE CONCAT('%', #{keyword}, '%')
            OR content LIKE CONCAT('%', #{keyword}, '%')
        )
        ORDER BY create_time DESC
    </select>

    <!-- 搜索服务门店 -->
    <select id="searchServices" resultType="com.pethome.entity.ServiceStore">
        SELECT * FROM service_stores
        WHERE 1 = 1
        AND (
            store_name LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%')
        )
        ORDER BY created_at DESC
    </select>

</mapper>
