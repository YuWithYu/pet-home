<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pethome.mapper.ServiceAppointmentRatingMapper">

    <select id="findByAppointmentAndUser" resultType="com.pethome.entity.ServiceAppointmentRating">
        SELECT * FROM service_appointment_rating
        WHERE appointment_type = #{appointmentType}
          AND appointment_id = #{appointmentId}
          AND user_id = #{userId}
        LIMIT 1
    </select>

    <select id="avgRatingByMemberId" resultType="java.math.BigDecimal">
        SELECT ROUND(COALESCE(AVG(rating), 5.0), 1) FROM service_appointment_rating WHERE member_id = #{memberId}
    </select>
</mapper>
